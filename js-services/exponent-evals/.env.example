# Required: Tenant to run evals against
EVAL_TENANT_ID=abc123def456

# Required: MCP API Key (not JWT keys!)
MCP_API_KEY=your-mcp-api-key-here
MCP_BASE_URL=https://mcp.getgrapevine.ai

# Required: OpenAI
OPENAI_API_KEY=sk-...

# Optional: Model configuration
TASK_EXTRACTION_MODEL=gpt-5

# Optional: Debug mode
DEBUG=false

# Required for single-agent strategy (with Grapevine search)
LINEAR_API_KEY=lin_api_...
LINEAR_TEAM_ID=<your-linear-team-uuid>
LINEAR_TEAM_NAME=Engineering

SEARCH_PROVIDER=linear-api
