apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-outbound-internet
  namespace: gather-gpt
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/name: coworker
  policyTypes:
    - Egress
  egress:
    - to:
        - ipBlock:
            cidr: 0.0.0.0/0
